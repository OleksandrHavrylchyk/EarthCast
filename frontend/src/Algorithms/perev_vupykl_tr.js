import {pointArray, t1, t2, t3, v1, v2, v3, v, flag_vupykl_tr} from "../Services/constantService";
import kosy from "./kosy";

export default function perev_vupykl_tr() {
    if (flag_vupykl_tr == 1) {
        // let ro= kosy(pointArray[rob[head+1]] - pointArray[rob[head]], pointArray[rob[head+2]] - pointArray[rob[head+1]]);

        // if (k - head >= 3) {
        //     for(let i = head + 2; i < k; i++) {
        //         ro *= kosy(pointArray[rob[i]] - pointArray[rob[i-1]], pointArray[rob[i+1]] - pointArray[rob[i]]);

        //         if(ro < 0) {
        //             return 0;
        //         }
        //     }
        // }
    }

    return 1;
}